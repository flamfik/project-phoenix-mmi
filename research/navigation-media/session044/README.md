# Session 044 publication-safe evidence

`lod-yim-corpus.public.json` records the content-deduplicated LOD/YIM corpus,
cross-disc memberships, sizes and aggregate morphology.

It excludes content hashes, raw headers, payload bytes, decoded rasters,
strings and local paths. Private working data belongs under ignored `work/`.
