# Session 039 publication-safe evidence

Generated reports:

- `cd1-registered-provenance.public.json`;
- `cd3-registered-provenance.public.json`;
- `cd1-cd3.registered-provenance.comparison.json`;
- `registered-provenance-correlation.json`.

They contain artifact hashes, the fixed Session 038 component interval,
registered family counts, fixed distance classes, nearest-evidence distances,
the `RB-015` contextual relation and conservative evidence statuses.

Firmware/component bytes, instruction bytes, mnemonic names, raw strings,
pointer values, absolute runtime addresses, local paths, extracted resources
and map payloads are excluded. The private full analysis belongs under
ignored `work/`.
