# Session 047 publication-safe evidence

`yim-integrity.public.json` records only the fixed candidate matrix and match
counts for the two opaque preamble fields.

It contains no field values, raw bytes or decoded raster material. Repacking
remains explicitly blocked.
