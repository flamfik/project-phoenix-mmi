# Session 053 publication-safe evidence

`embedded-xim2-census.public.json` reports strict resource counts, geometry and
cross-version equality. Offsets, bytes, rasters and content hashes are absent.
