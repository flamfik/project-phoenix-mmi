# Session 046 publication-safe evidence

`yim-rle.public.json` records encoded/decoded sizes, bounded command counts
and compression ratios.

The five decoded 480 by 240 rasters and their hashes are private and are not
written by the runner.
