# Session 029 publication-safe evidence

Generated reports:

- `cd1-handoff-mapping.public.json`;
- `cd3-handoff-mapping.public.json`;
- `cd1-cd3.handoff-mapping.comparison.json`;
- `handoff-mapping-correlation.json`.

They contain hashes, file-relative offsets, prefix lengths, normalized shape
hashes, generated classifications and aggregate counts only. Firmware bytes,
instruction bytes, raw pointer values, absolute runtime addresses, raw
strings, local paths, map payloads and extracted resources are excluded. Full
local output belongs under ignored `work/`.
