# Session 027 publication-safe evidence

Generated reports:

- `cd1-producer-return-family.public.json`;
- `cd3-producer-return-family.public.json`;
- `cd1-cd3.producer-return-family.comparison.json`;
- `producer-return-family-correlation.json`.

They contain hashes, file-relative offsets, generated expressions, field
geometry and counts only. Firmware bytes, instruction bytes, runtime
addresses, raw strings, local paths, map payloads and extracted resources are
excluded. Full local output belongs under ignored `work/`.
