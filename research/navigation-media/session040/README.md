# Session 040 publication-safe evidence

Generated reports:

- `cross-payload-homolog.comparison.json`;
- `cd1-cross-payload-homolog.public.json`;
- `cd2-cross-payload-homolog.public.json`;
- `cd3-cross-payload-homolog.public.json`;
- `cross-payload-homolog-correlation.json`.

They contain artifact hashes, anchor geometry and quality metrics, corpus
counts, content-deduplication totals, target/control match counts, saturation
status and conservative classifications.

Firmware, payload, component and signature bytes; signature hashes;
instruction bytes; mnemonic names; raw strings; pointer values; absolute
runtime addresses; local paths; extracted resources and map payloads are
excluded. The private full analysis belongs under ignored `work/`.
