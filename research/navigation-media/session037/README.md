# Session 037 publication-safe evidence

Generated reports:

- `cd1-micro-island.public.json`;
- `cd3-micro-island.public.json`;
- `cd1-cd3.micro-island.comparison.json`;
- `micro-island-correlation.json`.

They contain artifact hashes, the strictly derived 2 KiB overlap, the two
prior deltas, fixed microbin and enrichment parameters, generated IDs,
file-relative offsets, exact-run/unit counts, anonymous decoder morphology and
evidence statuses.

Firmware bytes, raw overlap bytes, word values, instruction bytes, mnemonic
names, raw strings, pointer values, absolute runtime addresses, local paths,
map payloads and extracted resources are excluded. The full private analysis
belongs under ignored `work/`.
