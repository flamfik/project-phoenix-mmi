# Session 030 publication-safe evidence

Generated reports:

- `cd1-literal-pool-boundary.public.json`;
- `cd3-literal-pool-boundary.public.json`;
- `cd1-cd3.literal-pool-boundary.comparison.json`;
- `literal-pool-boundary-correlation.json`.

They contain hashes, file-relative pool boundaries, target indices, relative
referrer/use roles and aggregate counts only. Firmware bytes, instruction
bytes, raw pointer values, absolute runtime addresses, raw strings, local
paths, map payloads and extracted resources are excluded. Full local output
belongs under ignored `work/`.
