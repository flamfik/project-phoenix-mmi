# Session 028 publication-safe evidence

Generated reports:

- `cd1-handoff-field60.public.json`;
- `cd3-handoff-field60.public.json`;
- `cd1-cd3.handoff-field60.comparison.json`;
- `handoff-field60-correlation.json`.

They contain hashes, file-relative offsets, generated expressions, mnemonic
names and aggregate counts only. Firmware bytes, instruction bytes, absolute
runtime addresses, raw strings, local paths, map payloads and extracted
resources are excluded. Full local output belongs under ignored `work/`.
