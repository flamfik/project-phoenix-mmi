# Session 033 publication-safe evidence

Generated reports:

- `cd1-relocation-descriptors.public.json`;
- `cd3-relocation-descriptors.public.json`;
- `cd1-cd3.relocation-descriptors.comparison.json`;
- `relocation-descriptor-correlation.json`.

They contain artifact hashes, generated zone IDs, file-relative offsets,
closed search parameters, counts, candidate histograms and evidence statuses
only. Firmware bytes, instruction bytes, raw pointer values, absolute runtime
addresses, raw strings, local paths, map payloads and extracted resources are
excluded. Full local output belongs under ignored `work/`.
