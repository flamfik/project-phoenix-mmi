# Session 016 public evidence

This directory contains deterministic, publication-safe reports from the
bounded predecessor-context and dynamic descriptor analysis.

Files:

- `cd1-object-dispatch.public.json`;
- `cd3-object-dispatch.public.json`;
- `cd1-cd3.object-dispatch.comparison.json`;
- `object-dispatch-correlation.json`.

The reports contain no firmware or instruction bytes, absolute runtime
addresses, raw strings, local paths, map payloads or extracted resources.

Result: four unique paired unresolved call sites reduce to two contextual
literal optical target pairs and two instances of one cross-version dynamic
descriptor shape. Neither literal target pair passes the independent graph-
expansion code gate. No navigation-to-optical edge, sector-read ABI, buffer
owner, buffer provenance or FLDB parser is established.
