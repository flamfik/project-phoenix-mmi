# Session 031 publication-safe evidence

Generated reports:

- `cd1-piecewise-link-map.public.json`;
- `cd3-piecewise-link-map.public.json`;
- `cd1-cd3.piecewise-link-map.comparison.json`;
- `piecewise-link-map-correlation.json`.

They contain artifact hashes, generated link-pair IDs, cross-release relocation
deltas, aggregate counts, bounded file-relative candidate offsets, normalized
structural hashes and evidence statuses only. Firmware bytes, instruction
bytes, raw pointer values, absolute runtime addresses, raw strings, local
paths, map payloads and extracted resources are excluded. Full local output
belongs under ignored `work/`.
