# Session 042 publication-safe evidence

Generated reports:

- `distributed-homolog.comparison.json`;
- `raw-distributed-homolog.public.json`;
- `record-normalized-distributed-homolog.public.json`;
- `distributed-homolog-correlation.json`.

They contain the fixed subanchor geometry and quality metrics, reproduced
corpus counts, separate raw/normalized domain results, conservative
classifications and operational graph v35.

Firmware, payload, decoded-region, component and signature bytes; signature
and payload hashes; instruction bytes; mnemonic names; raw strings; pointer
and address values; absolute runtime addresses; local paths; extracted
resources and map payloads are excluded. Private analysis belongs under
ignored `work/`.
