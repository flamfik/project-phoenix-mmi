# Session 038 publication-safe evidence

Generated reports:

- `cd1-run-gap-topology.public.json`;
- `cd3-run-gap-topology.public.json`;
- `cd1-cd3.run-gap-topology.comparison.json`;
- `run-gap-topology-correlation.json`.

They contain artifact hashes, the prior 2 KiB overlap, fixed gap/component and
period gates, generated IDs, file-relative offsets, run/gap histograms,
aggregate counts, ratios and evidence statuses.

Firmware bytes, unequal byte values, word values, instruction bytes, mnemonic
names, raw strings, pointer values, absolute runtime addresses, local paths,
map payloads and extracted resources are excluded. The private full analysis
belongs under ignored `work/`.
