# Session 041 publication-safe evidence

Generated reports:

- `record-normalized-homolog.comparison.json`;
- `cd1-record-normalized-homolog.public.json`;
- `cd2-record-normalized-homolog.public.json`;
- `cd3-record-normalized-homolog.public.json`;
- `record-normalized-homolog-correlation.json`.

They contain source/format counts, record-validation metrics, decoded-region
counts and sizes, target/control match counts, conservative classifications
and operational graph v34.

Firmware, payload, decoded-region, component and signature bytes; signature
and decoded-payload hashes; instruction bytes; mnemonic names; raw strings;
pointer and address values; absolute runtime addresses; local paths;
extracted resources and map payloads are excluded. Private analysis belongs
under ignored `work/`.
