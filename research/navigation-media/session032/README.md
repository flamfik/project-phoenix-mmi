# Session 032 publication-safe evidence

Generated reports:

- `cd1-relocation-breakpoints.public.json`;
- `cd3-relocation-breakpoints.public.json`;
- `cd1-cd3.relocation-breakpoints.comparison.json`;
- `relocation-breakpoint-correlation.json`.

They contain artifact hashes, generated anchor/zone IDs, file-relative offsets,
lengths, cross-release deltas, bounded breakpoint brackets, structural hashes
and evidence statuses only. Firmware bytes, instruction bytes, raw pointer
values, absolute runtime addresses, raw strings, local paths, map payloads and
extracted resources are excluded. Full local output belongs under ignored
`work/`.
