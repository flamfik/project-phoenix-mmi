# Session 035 publication-safe evidence

Generated reports:

- `cd1-dual-delta-similarity.public.json`;
- `cd3-dual-delta-similarity.public.json`;
- `cd1-cd3.dual-delta-similarity.comparison.json`;
- `dual-delta-similarity-correlation.json`.

They contain artifact hashes, the Session 034 envelope, two prior relocation
deltas, fixed window/grid parameters, thresholds, aggregate dominance counts,
run/crossing bounds and evidence statuses. Firmware bytes, full per-window
samples, raw strings, raw pointer values, absolute runtime addresses, local
paths, map payloads and extracted resources are excluded.

The comparison report retains the primary compact run topology. Per-disc
reports retain only aggregate scale summaries. Full origin-grid and shifted-
grid window samples belong under ignored `work/`.
