# Session 008 publication-safe evidence

This directory contains generated evidence for the relocated bitmap region and the confidence-graded firmware operational model.

- `cd1-firmware-operational-model.public.json` - MMI 5150 atlas metrics and prior-evidence summary;
- `cd3-firmware-operational-model.public.json` - MMI 5570 atlas metrics and prior-evidence summary;
- `cd1-cd3.firmware-operational-model.comparison.json` - relocated equality, classifier controls and the combined operational graph.

The reports contain hashes, offsets, counts, fixed thresholds and aggregate bit morphology only. They contain no firmware bytes, rendered glyphs, resources, raw pointer runs or arbitrary extracted strings. Full local reports remain under the ignored `research/firmware-5570/work/session008/` directory.
