# Session 004 publication-safe evidence

- `cd1-executable-layout.public.json` - MMI 5150 startup semantics, fixed VxWorks probes, address-model results and resource-island boundaries;
- `cd3-executable-layout.public.json` - corresponding MMI 5570 evidence;
- `cd1-cd3.executable-layout.comparison.json` - sanitized cross-version comparison.

The files contain no firmware bytes, instruction encodings, extracted resource bytes, arbitrary raw strings, vehicle data or Component Protection material. Full local reports remain under ignored `research/firmware-5570/work/session004/`.
