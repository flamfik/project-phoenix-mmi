# Session 003 publication-safe evidence

These files were generated by Phoenix SDK from ISO images whose SHA-256 values match the project artifact register.

- `cd1-mmi.public-summary.json` — MMI 5150 hashes, offsets, entropy, fixed marker metadata, validated resource metadata and checksum layout;
- `cd3-mmi.public-summary.json` — the corresponding MMI 5570 evidence;
- `cd1-cd3.comparison.json` — sanitized comparison, including shared resource hashes.

The summaries contain no firmware bytes, exported resource bytes, arbitrary raw strings, vehicle data or Component Protection material. Full local reports remain under ignored `research/firmware-5570/work/session003/`.

Reproduce them with the command in `docs/sessions/SESSION-003-mmi-bin-static-analysis.md`.
